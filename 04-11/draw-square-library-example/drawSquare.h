#include <iostream> // now I can use cout
using namespace std;

// drawSquare.h is a *header* file. It contains the declarations of any functions 
// that are in this library.

// drawSquare is a function that just *does* something
// the type of functions that do things, but don't actually return things is called "void"
// you don't need to return anything in a void-type function
void drawSquare(int size);