#ifndef SHAPES_H // this ensures that shapes.h is only ever included once (no infinite loops of inclusion)
#define SHAPES_H
#include <string>
using namespace std;

string drawTriangle(int n); // declaration for drawTriangle
#endif